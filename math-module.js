const sum = (num1, num2) => num1 + num2;


sum(2, 3)

module.exports {
    sum: sum
}

const sum = (a, b) => a + b;

const multiply = (a, b) => a * b;

module.exports = {
  sum: sum,
  multiply: multiply
}